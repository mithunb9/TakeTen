import React, { useState } from "react";
import Modal from "./Modal";

const Navbar = () => {
  return (
    <div>
      <div className=" bg-[url('../components/images/take10logo.png')] bg-contain bg-no-repeat text-black h-[10vh]"/>
    </div>
  );
};

export default Navbar;
