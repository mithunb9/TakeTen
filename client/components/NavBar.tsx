import React, { useState } from "react";
import Modal from "./Modal";
import Image from "next/image";

const Navbar = () => {
  return (
    <div className="">
      <div className=" bg-[url('../components/images/take10logo.png')] bg-contain bg-no-repeat text-black h-[12vh] w-auto" />
    </div>
  );
};

export default Navbar;
